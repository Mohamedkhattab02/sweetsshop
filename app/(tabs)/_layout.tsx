/**
 * The tab navigator itself is platform-split: a native UITabBar (Liquid Glass)
 * on iOS, a Material 3 navigation bar on Android. Metro picks the right one.
 */

export { MarketTabs as default } from '@/components/navigation/market-tabs';
