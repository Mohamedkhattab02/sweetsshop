// Stable, explicit route used by EXIT buttons. Keeping the role selector at a
// named path avoids ambiguity with the customer tab group, which also has a
// root-like index screen.
export { default } from './index';
